//asked to code take a string and every concurrent character should be converted to it upperCaseonly if previous character is not an vowel (space also counted)
function str(data){
    let result=''
    
}
console.log(str('hello world'))